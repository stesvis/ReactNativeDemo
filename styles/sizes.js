const FontSizes = {
  bigFontSize: 18,
  biggerFontSize: 20,
  defaultFontSize: 16,
  smallFontSize: 13,
};

const Sizes = {
  footerFontSize: FontSizes.smallFontSize,
  textFontSize: FontSizes.defaultFontSize,
  navbarTitleFontSize: FontSizes.biggerFontSize,
};

export { FontSizes, Sizes };
